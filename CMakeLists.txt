cmake_minimum_required(VERSION 2.8)
project (FEEDBACK)

include(${ARDUINO_SETTINGS_FILE})
 
set (PROJECT_INCLUDE_DIR ${PROJECT_SOURCE_DIR}/include)
set (PROJECT_SOURCE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/src)
 
file(GLOB FEEDBACK_SRCS ${PROJECT_SOURCE_DIR}/*.cpp ${PROJECT_SOURCE_DIR}/*.c)
file(GLOB AMC7812_SRCS ${CMAKE_SOURCE_DIR}/amc7812/src/*.cpp )

message(STATUS ${FEEDBACK_SRCS})
message(STATUS ${AMC7812_SRCS})


include_directories("${PROJECT_BINARY_DIR}")
include_directories("${PROJECT_INCLUDE_DIR}")
 
add_avr_library(${PROJECT_NAME} STATIC ${FEEDBACK_SRCS} ${AMC7812_SRCS})
